<h4>Now I have to think about read.md now </h4>
>f**k,my 2FA had lost,now I have to use new account.(By the way, the user [@xugoudaobai](https://github.com/xugoudaobai) does not means xugoudaobAI)

<h5>This is a module I made for fun(?).The word("Deadly")'s pronounciation sounds like "dai de lei",which means something abstract(Also,some functions in it might be truly deadly at some conditions)</h5>
I wrote it as a learning experience in Python.It could be used in some beginners in Python.

Use this after install the repo to build the module:(Don't "python deadly.py ", please.)
```bash
pip install build setuptools wheel
#you can also use python -m pip install build setuptools
#Find the Project and
#cd to-the-repo
python -m build
```

or

```bash
#will be deprecated
pip install setuptools wheel
#you can also use python -m pip install setuptools
#Find the Project and
#cd to-the-repo
python setup.py sdist bdist_wheel
```

Anyway,don't try "python deadly.py".

The project(or this repo)are all written by myself,so I only write some essential annotation in the program...
